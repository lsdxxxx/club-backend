server:
  port: 8080

spring:
  application:
    name: club-backend
  datasource:
    url: jdbc:mysql://localhost:3306/university_club_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: remote_user
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.example.demo.entity